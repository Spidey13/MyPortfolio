import { oceanDepthsPreset } from 'react-ai-orb'

// Using the default ocean depths preset from react-ai-orb

// Hero section orb configuration - using default ocean depths
export const heroOrbConfig = {
  ...oceanDepthsPreset,
  size: 1.5,
}

// Breathing orb (AI co-pilot) configuration - using default ocean depths  
export const breathingOrbConfig = {
  ...oceanDepthsPreset,
  size: 0.8,
} 